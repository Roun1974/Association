{% extends 'base.html.twig' %}

{% block title %}Réinitialiser votre mot de passe
{% endblock %}

{% block body %}

<div class="container mt-5 mb-5" style="max-width: 800px;">
            <div class="row">
                 <div class="card p-3">
                    <div class="col">
                       <h4 class="lead">Réinitialiser votre mot de passe</h4>
    
    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="btn btn-primary">Réinitialiser</button>
    {{ form_end(resetForm) }}
                   </div>
              </div>
        </div>  
 </div>
{% endblock %}

